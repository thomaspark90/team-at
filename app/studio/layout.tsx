// 스튜디오 섹션 공통 — 클라이언트 하위 페이지(IG 메뉴 업데이트 등)의 탭 제목 기본값
export const metadata = { title: 'Staff Meal' };

export default function StudioLayout({ children }: { children: React.ReactNode }) {
  return children;
}
